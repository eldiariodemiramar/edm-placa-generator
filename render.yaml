services:
  - type: web
    name: edm-placa-generator
    runtime: python
    buildCommand: pip install -r requirements.txt && python app.py --download-assets || true
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
